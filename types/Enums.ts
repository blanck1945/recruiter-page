export enum RowsEnums {
  one = 'one',
  two = 'two',
  three = 'three',
}

export enum LayoutValueEnum {
  empresa = 'empresa',
  recruiter = 'recruiter',
  empleo = 'empleo',
}

export enum ButtonEnum {
  navWhite = 'nav-white',
  navOrage = 'nav-orange',
  sumate = 'sumate',
}
